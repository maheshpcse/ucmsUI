import { DataTruncatePipe } from './data-truncate.pipe';

describe('DataTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new DataTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
