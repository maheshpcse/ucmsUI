import { DataTransferPipe } from './data-transfer.pipe';

describe('DataTransferPipe', () => {
  it('create an instance', () => {
    const pipe = new DataTransferPipe();
    expect(pipe).toBeTruthy();
  });
});
